import styled, { css } from 'styled-components'

export const Aside = styled.aside`
  padding: 16px;
  background-color: #81ecec;
  height: 100vh;
  width: 240px;

  ${css`
    position: fixed;
    width: 240px;
  `}
`
